// // aria function
// exports.aria = (width, height) => width * height;
// //circumference
// exports.circumference = (width, height) => 2 * (width + height);

module.exports = {
    aria: (width, height) => width * height,
    circumference: (width, height) => 2 * (width + height),
    currentTime: Date(),
};